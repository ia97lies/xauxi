+ maintenance
  + session plugin: remove old sessions
  + file cache for certs
  + hook ssl to gain more infos (reneg certs...)

+ debugger
  + remote
  + only config shield code

+ authentication
  + source
    + passwd
    + remote

+ deflate

+ ssl

