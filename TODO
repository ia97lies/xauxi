+ maintenance

+ debugger
  + remote
  + only config shield code

+ authentication
  + source
    + passwd
    + remote

+ deflate

+ ssl
  + hook ssl to gain more infos (reneg certs...)

+ Overwrit res:write, res:finish, res:writeHead functions
  + on finish write transfer.log else stuff written directly in config will lost a transferlog entry
